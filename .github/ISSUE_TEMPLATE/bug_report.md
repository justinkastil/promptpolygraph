---
name: Bug report
about: Report a defect in PromptPolygraph
title: ""
labels: bug
---

**What happened**
A clear description of the bug.

**To reproduce**
Steps or a minimal config/command. A `--mock` repro is ideal (no API key needed).

**Expected behavior**
What you expected instead.

**Environment**
- PromptPolygraph version (`polygraph --version` or `pip show promptpolygraph`):
- Python version:
- OS:
- Relevant extras installed (`service`, `redteam`, `crypto`, `oidc`, `litellm`):

**Logs / output**
Paste any traceback or relevant output.

> Do not report security vulnerabilities here — use the security policy
> (Security → Report a vulnerability). See SECURITY.md.
